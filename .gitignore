# VeVit Tools — statické HTML/CSS/JS/PHP (žádné závislosti, žádný build).

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env (lokální konfigurace Ollamy se nepotvrzuje do repa)
.env
.env.*

# Případné dočasné logy
*.log
