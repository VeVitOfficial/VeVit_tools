Options -Indexes
RewriteEngine On

# Block direct access to config_secret.php
<Files "config_secret.php">
    Order Allow,Deny
    Deny from all
</Files>

# Everything else passes through normally
# index.html is served automatically